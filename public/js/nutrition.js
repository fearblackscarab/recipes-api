const express=require('express');
const router=express.Router();
const nutritionDiv=document.querySelectorAll('.nutrition-div');

const recipeRoute=require('../../routes/api/recipeRoute')
nutritionDiv.forEach(item=>{
    
})